export type Signal = {
  category: string;
  title: string;
  description: string;
  velocity: string;
  horizon: string;
  source: string;
  score: number;
  updatedAt: string;
  sourceUrl?: string;
};
